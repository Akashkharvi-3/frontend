/* import React from 'react';

const Circle = ({ size, color, key }) => {
  const style = {
    width: size,
    height: size,
    borderRadius: '550%',
    backgroundColor: color,
    margin: '5px',
  };

  return <div style={style} key={key}></div>;
};

export default Circle; */